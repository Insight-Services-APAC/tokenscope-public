{
  "description": "TokenScope Copilot session hooks — emit-on-install enrolment, and the legacy per-project file forwarder, which runs only for a project with a span file (sessions from a terminal that still exports COPILOT_OTEL_FILE_EXPORTER_PATH).",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "node \"${COPILOT_PLUGIN_ROOT}/hooks/forwarder-lifecycle.mjs\" start",
            "timeout": 15
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "node \"${COPILOT_PLUGIN_ROOT}/hooks/forwarder-lifecycle.mjs\" stop",
            "timeout": 30
          }
        ]
      }
    ]
  }
}
